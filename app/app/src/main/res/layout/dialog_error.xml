<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Error Icon and Message -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/ivErrorIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_error"
            android:contentDescription="@string/error"
            app:tint="?colorError" />

        <TextView
            android:id="@+id/tvErrorMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:textAppearance="?textAppearanceBodyLarge"
            tools:text="An error occurred while processing the file." />

    </LinearLayout>

    <!-- Expandable Details Section -->
    <LinearLayout
        android:id="@+id/layoutDetailsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp"
        android:visibility="gone">

        <!-- Expand/Collapse Header -->
        <LinearLayout
            android:id="@+id/layoutDetailsHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingVertical="8dp"
            android:background="?selectableItemBackground">

            <ImageView
                android:id="@+id/ivExpandIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_expand_more"
                android:contentDescription="@string/expand_details"
                app:tint="?colorOnSurfaceVariant" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="@string/show_details"
                android:textAppearance="?textAppearanceLabelLarge"
                android:textColor="?colorPrimary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCopyDetails"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:icon="@drawable/ic_content_copy"
                android:contentDescription="@string/copy_to_clipboard" />

        </LinearLayout>

        <!-- Details Content -->
        <androidx.core.widget.NestedScrollView
            android:id="@+id/scrollDetails"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxHeight="200dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvErrorDetails"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="12dp"
                android:background="@drawable/bg_code_block"
                android:fontFamily="monospace"
                android:textAppearance="?textAppearanceBodySmall"
                android:textColor="?colorOnSurfaceVariant"
                android:textIsSelectable="true"
                tools:text="java.io.IOException: Failed to read file\n    at com.sza.fastmediasorter..." />

        </androidx.core.widget.NestedScrollView>

    </LinearLayout>

    <!-- Action Suggestions (optional) -->
    <TextView
        android:id="@+id/tvSuggestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="@drawable/bg_info_card"
        android:textAppearance="?textAppearanceBodyMedium"
        android:textColor="?colorOnSurface"
        android:visibility="gone"
        android:drawableStart="@drawable/ic_lightbulb"
        android:drawablePadding="8dp"
        tools:text="Try checking if the file is accessible or not locked by another application." />

</LinearLayout>
