# Release Signing Configuration Template
# Copy this file to keystore.properties and fill in your values
# WARNING: keystore.properties is in .gitignore - never commit it!

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=fastmediasorter
storeFile=../fastmediasorter.keystore

# To generate a new keystore:
# keytool -genkey -v -keystore fastmediasorter.keystore -alias fastmediasorter -keyalg RSA -keysize 2048 -validity 10000
